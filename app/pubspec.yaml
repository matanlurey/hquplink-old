name: hquplink
description: A Star Wars Legion App.
version: 0.1.0-alpha

environment:
  sdk: ">=2.0.0-dev.68.0 <3.0.0"

dependencies:
  cupertino_icons: ^0.1.2
  firebase_auth: ^0.6.6
  flutter:
    sdk: flutter
  google_sign_in: ^3.2.4
  swlegion:
    path: ../database/swlegion

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  assets:
    - assets/
  uses-material-design: true
